import type { JSX } from 'react';

/**
 * Root application component.
 *
 * Placeholder shell — real views are built out via the spec-driven flow.
 */
export function App(): JSX.Element {
  return <h1>lametrader</h1>;
}
