#!/usr/bin/env node
/**
 * Executable entry point for the `lametrader` CLI.
 *
 * Kept thin: parse argv, dispatch to a command, exit. Command logic is built
 * out via the spec-driven flow.
 */
export {};
